### build

```bash
$ poetry install
$ poetry shell

$ python main.py --help                                    
usage: main.py [-h] --train TRAIN --test TEST

optional arguments:
  -h, --help     show this help message and exit
  --train TRAIN  path to train
  --test TEST    path to test


```